using System;

namespace Faker.Provider.CHS
{
    public class ProviderFactory : ProviderFactoryBase
    {
        public ProviderFactory(IServiceProvider serviceProvider) : base(serviceProvider)
        {
        }
    }
}